; 符号key
{foo: :bar}
; 变量标识符key
(let foo :hello)
{.foo: :world}

(let foo "great value")
(let bar {(. foo):"is me"})
; (. foo)可以简写为.foo，所以上述可写为：
(let bar {.foo:"is me"})

(let {foo:foo1, bar, key1,key2, @all,} mydict)
